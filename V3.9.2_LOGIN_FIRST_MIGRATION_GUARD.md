# VEREDICTA v3.9.2

Correções:

1. LOGIN-FIRST: abrir `/` sempre exibe a tela de login. O painel só é mostrado depois de submissão bem-sucedida da senha.
2. O backend detecta ausência de `cases.owner_id` e carrega a listagem em modo de compatibilidade, em vez de quebrar o painel.
3. O modo multiusuário completo continua exigindo a migration `supabase/migration_v3_9_multiuser.sql`.
4. O validator jurídico permanece em 3.8.1.
